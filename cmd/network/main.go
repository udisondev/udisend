// Command network runs a udisend network node (signaling-relay, optional STUN/TURN volunteer, DHT participant).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stdout, "TODO: udisend network node")
}
